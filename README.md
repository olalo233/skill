# skill

Personal reusable Agent Skills.

## Skills

- `web-retrieve` — Minimal public web retrieval fallback: normal request → one browser-like retry → another public source. Keeps retrieval behavior isolated from HTTP/API debugging.
